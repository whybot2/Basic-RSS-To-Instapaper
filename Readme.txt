BASIC RSS TO INSTAPER

Simple python scipt to collect RSS feeds, check if already sent to instapaper then sends new urls.
Uses instaper basis API
A simple database file is created that saves all the urls from feeds once pushed.
A basic log file is also created.

Run as a cron job to periodically check for updates